# belirtec-embed — training + evaluation environment
# =====================================================================
# KNOWN-GOOD PINNED STACK — this exact set trained the full 12-experiment
# matrix with ZERO NaN. Do not upgrade torch / transformers / sentence-
# transformers casually: bleeding-edge versions caused NaN training collapse
# twice in this project —
#   (1) transformers 5.x + torch 2.11  -> grad_norm=nan at step 1;
#   (2) torch >= 2.8 silently flips the HF Trainer default optimizer to
#       adamw_torch_fused, whose fused kernels have a NaN history
#       (pytorch/pytorch#95781).
# The loop pins optim="adamw_torch" and installs a non-finite-gradient guard,
# but the version pins below are the primary defense.
#
# Install:
#   conda create -n belirtec python=3.11 -y && conda activate belirtec
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt
#   pip install -e .
#
# NOTE ON TORCH: torch is NOT listed below because it needs the CUDA wheel
# index (--index-url .../cu124) which a plain `pip install -r` cannot express.
# Install it first with the command above. Pinned version: torch==2.5.1+cu124.

# --- the pinned trio (the NaN fix — keep these exact) ---
transformers==4.49.0
sentence-transformers==4.1.0
peft==0.19.1

# --- training ---
accelerate==1.5.2
datasets==3.6.0

# --- evaluation (mteb 1.x API is required by the vendored mteb_eval.py;
#     this version is calibrated vs the Mizan leaderboard: +0.20 MTEB / -0.25 Legal) ---
mteb==1.39.7
scikit-learn==1.9.0
scipy==1.17.1
pandas==3.0.3

# --- io / utils ---
huggingface-hub==0.36.2
safetensors==0.8.0
pyyaml==6.0.3
numpy==2.4.6
tqdm==4.68.3
