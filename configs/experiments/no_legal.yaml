loss:
  per_dataset: {contrastive: gist}
data:
  buckets:
    legal:          {enabled: false}
    retrieval:      {enabled: true}
    sts:            {enabled: true}
    classification: {enabled: true}
output_dir: ./runs/no_legal
